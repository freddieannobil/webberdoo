{% extends 'App/layout/admin_base.html.twig' %}

{% block body %}
    <h1>Post edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-primary" formnovalidate/>
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('admin_post_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <button type="submit" class="btn btn-primary" formnovalidate><i class="fa fa-trash" aria-hidden="true"></i></button>
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
