{% extends 'App/layout/inc/admin_box_layout.html.twig' %}


{% block title %}Channel creation{% endblock %}

{% block form %}
    <!-- form start -->
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" class="btn btn-primary btn-lg btn-block" formnovalidate/>
    {{ form_end(form) }}

{% endblock %}

{% block links %}

    <ul>
        <li>
            <a href="{{ path('admin_channel_index') }}" class="btn btn-default btn-lg btn-block">Back to the list</a>
        </li>
    </ul>

{% endblock %}
